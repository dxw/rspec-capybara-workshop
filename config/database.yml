default: &default
  adapter: postgresql
  url: postgres://postgres@localhost:5432/rspec-capybara-workshop
  encoding: unicode
  template: template0
  pool: <%= Integer(ENV.fetch('RAILS_MAX_THREADS') { 5 }) %>

development:
  <<: *default

test:
  <<: *default
  url: postgres://postgres@localhost:5432/rspec-capybara-workshop_test

production:
  <<: *default
